/**
 * 📊 TRADING TYPES
 * Consolidated type definitions for trading-related functionality
 */

/**
 * Signal types that can be generated by trading strategies
 */
export enum SignalType {
  LONG = 'LONG',
  SHORT = 'SHORT',
  EXIT = 'EXIT',
  NEUTRAL = 'NEUTRAL',
}

/**
 * Distribution channels for strategies
 */
export enum DistributionChannel {
  DISCORD = 'DISCORD',
  DESKTOP_APP = 'DESKTOP_APP',
  MOBILE_APP = 'MOBILE_APP',
  EMAIL = 'EMAIL',
  WEB = 'WEB',
}

/**
 * Parameter for a trading strategy
 */
export interface StrategyParameter {
  id: string;
  name: string;
  description: string;
  type: 'number' | 'boolean' | 'string' | 'array' | 'object' | 'enum';
  defaultValue: any;
  range?: [number, number]; // For numeric parameters
  options?: string[]; // For enum parameters
  isRequired: boolean;
  isAdvanced: boolean;
}

/**
 * Definition of a trading strategy
 */
export interface StrategyDefinition {
  id: string;
  name: string;
  description: string;
  version: string;
  parameters: StrategyParameter[];
  tags: string[];
  supportedInstruments: string[];
  distributionChannels: DistributionChannel[];
  subscriptionTiers: string[]; // Which subscription tiers have access
  isActive: boolean;
  createdAt?: Date;
  updatedAt?: Date;
}

/**
 * Trading signal generated by a strategy
 */
export interface TradingSignal {
  id: string;
  strategyId: string;
  instrumentId: string;
  timestamp: number;
  type: SignalType;
  confidence: number;
  price?: number;
  targetPrice?: number;
  stopPrice?: number;
  metadata: Record<string, any>;
  createdAt?: Date;
}

/**
 * Strategy instance with specific parameter values
 */
export interface StrategyInstance {
  id: string;
  strategyDefinitionId: string;
  userId: string;
  name: string;
  parameters: Record<string, any>;
  isActive: boolean;
  createdAt?: Date;
  updatedAt?: Date;
}

/**
 * Strategy performance metrics
 */
export interface StrategyPerformance {
  strategyId: string;
  period: 'day' | 'week' | 'month' | 'year' | 'all';
  winRate: number;
  totalSignals: number;
  profitFactor: number;
  averageReturn: number;
  maxDrawdown: number;
  sharpeRatio: number;
  metadata: Record<string, any>;
  updatedAt: Date;
}

/**
 * Subscription tier information
 */
export interface SubscriptionTier {
  id: string;
  name: string;
  description: string;
  price: number;
  billingPeriod: 'monthly' | 'quarterly' | 'annual';
  features: string[];
  strategyAccess: string[]; // Array of strategy IDs
  maxInstances: number;
  isActive: boolean;
}
