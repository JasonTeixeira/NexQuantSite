"use client"

import React from 'react'
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

const UnifiedStrategySystem = () => {
  return (
    <Card className="bg-[#1a1a2e] border-cyan-700/30">
      <CardHeader>
        <CardTitle className="text-cyan-400">Unified Strategy System</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-300">Strategy system loading...</p>
      </CardContent>
    </Card>
  )
}

export default UnifiedStrategySystem
